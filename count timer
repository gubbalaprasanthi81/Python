import time

seconds = int(input("Enter seconds: "))

while seconds:
    mins = seconds // 60
    secs = seconds % 60
    print(f"{mins:02}:{secs:02}")
    time.sleep(1)
    seconds -= 1

print("Time's up!")
