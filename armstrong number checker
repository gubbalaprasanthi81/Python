num = int(input("Enter number: "))
power = len(str(num))
temp = num
total = 0

while temp > 0:
    digit = temp % 10
    total += digit ** power
    temp //= 10

print("Armstrong" if total == num else "Not Armstrong")
