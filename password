pwd = input("Enter password: ")

strength = (
    len(pwd) >= 8 and
    any(c.isupper() for c in pwd) and
    any(c.islower() for c in pwd) and
    any(c.isdigit() for c in pwd)
)

print("Strong Password" if strength else "Weak Password")
