nums = list(map(int, input("Enter numbers: ").split()))
count = 0

for i in range(len(nums)):
    if nums[i] > nums[(i+1) % len(nums)]:
        count += 1

print("Circular Sorted" if count <= 1 else "Not Circular Sorted")
